<div class="content-layout" id="login-page" layout="column" ng-controller="loginCtrl" ng-cloak>
    <h1>Reset your password</h1>
    <form ng-submit="login()" name="loginForm">
        <md-content>
            <md-input-container>
                <label>Enter a new password</label>
                <input ng-model="form.password" type="string" required>
            </md-input-container>
            <md-input-container>
                <label>Confirm your new password</label>
                <input ng-model="form.password" type="password" required>
            </md-input-container>
            <div id="submit-buttons">
                <md-button class="md-accent md-raised" type="submit" value="Submit" ng-disabled="loginForm.$invalid">Reset password</md-button>
            </div>
        </md-content>
    </form>
</div>